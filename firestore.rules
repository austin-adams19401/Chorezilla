rules_version = '2';

// TODO: TESTING ONLY - restore proper security rules before production release
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}